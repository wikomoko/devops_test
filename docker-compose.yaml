services:
  nginx:
    image: nginx:alpine
    container_name: hello-nginx
    restart: unless-stopped
    ports:
      - "8088:80"
    volumes:
      - ./html/index.html:/usr/share/nginx/html/index.html:ro